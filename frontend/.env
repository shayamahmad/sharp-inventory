# Leave empty: browser calls /api on the Vite port → Vite proxies to backend :3001
# Requires: npm run dev:full (or npm run api in another terminal)
# MongoDB Compass: NOT used by the browser — use backend/.env MONGODB_URI in Compass only
VITE_API_URL=
